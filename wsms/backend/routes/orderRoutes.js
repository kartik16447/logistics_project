const express = require("express");
const purchaseOrder = require("../controllers/createPurchaseOrder");

const router = express.Router();

// router.post("/", purchaseOrder.order_create_post);

module.exports = router;
